# Database
DB_URI=mongodb://localhost:27017/zeevx

# Server
PORT=8001
SECRET_KEY=your_session_secret_here

# Firebase - Option A: point to a service account JSON file (preferred)
# Absolute or relative path to service account JSON
# Example: FIREBASE_SERVICE_ACCOUNT_PATH=./Backend/Keys/serviceAccountKey.json
FIREBASE_SERVICE_ACCOUNT_PATH=

# Firebase - Option B: provide service account fields via env (use only if you cannot use a file)
FIREBASE_PROJECT_ID=
FIREBASE_CLIENT_EMAIL=
# IMPORTANT: when placing private key into .env, replace newlines with \n
# Example: FIREBASE_PRIVATE_KEY="-----BEGIN PRIVATE KEY-----\nMIIE...\n-----END PRIVATE KEY-----\n"
FIREBASE_PRIVATE_KEY=

# Optional: admin emails allowed to access console (comma separated)
ADMIN_EMAILS=admin@example.com
